export interface Ievent {
    name: string
    maxPeoples: number
    idPlace: string
    date: Date
    description: string
    address: string
    price: number
    willAttend: number
    images: string[]
}
