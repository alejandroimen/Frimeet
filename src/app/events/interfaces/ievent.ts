export interface Ievent {
    _id: string
    name: string
    maxPeoples: number
    idPlace: string
    date: Date
    description: string
    address: string
    price: number
    willAttend: number
    images: string[]
}
